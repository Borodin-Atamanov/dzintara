all:
  hosts:
    localhost:
      ansible_host: 127.0.0.1
      remote_user: i
      become: true
    pipyau:
      ansible_host: 192.168.43.60
      ansible_port: 22
      remote_user: root
      ansible_ssh_user: root
  vars:
      ansible_port: 30222
      host_key_checking: false
      ignore_errors: True      
#all passwords saved in "host_vars/<host_name>" files

#export ANSIBLE_HOST_KEY_CHECKING=False && ansible --inventory-file "ansible_hosts.yml" -m ping all -u root --ask-pass
#ansible --inventory-file "ansible_hosts.yml" -m ping all
