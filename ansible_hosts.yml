all:
  hosts:
    localhost:
      ansible_host: 127.0.0.1
    localhost2:
      ansible_host: 127.0.0.2
  vars:
      ansible_port: 30222
      host_key_checking: false
    


#export ANSIBLE_HOST_KEY_CHECKING=False && ansible --inventory-file "ansible_hosts.yml" -m ping all -u root --ask-pass
#ansible --inventory-file "ansible_hosts.yml" -m ping all -u i --ask-pass
