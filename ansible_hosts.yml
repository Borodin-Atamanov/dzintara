---
all: # keys must be unique, i.e. only one 'hosts' per group
	defaults:
		host_key_checking: false
	hosts:
		Pushok64:
			ansible_port: 30222
			ansible_host: 192.168.43.60
		Desktop64:
			ansible_port: 30222
			ansible_host: 127.0.0.1

#ansible --inventory-file "ansible_hosts.yml" -m ping all -u root --ask-pass
