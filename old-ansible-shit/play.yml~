- hosts: pipyau
  gather_facts: true
  tasks:
  - name: "Ansible | List all known variables and facts"
    debug:
      var: hostvars[inventory_hostname]
#        var: hostvars[ansible_ssh_pass]    
  - name: "Ansible | List all known variables and facts"
    debug:
      msg: "password for normal user = {{ansible_ssh_pass}}, password for become user {{ansible_become_password}}"

#  - name: Ensure that sshd is started
#    ansible.builtin.service:
#      name: sshd
#      state: started
      
#ansible-playbook --inventory-file "ansible_hosts.yml" play.yml


