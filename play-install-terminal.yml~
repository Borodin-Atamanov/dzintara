- hosts: localhost
  gather_facts: true
  remote_user: i
  become: yes 
  become_user: root
  become_method: sudo
  tasks:
#  - name: "Ansible | List all known variables and facts"
#    debug:
#      var: hostvars[inventory_hostname][ansible_ssh_pass]
#  - name: "Ansible | List all known variables and facts"
#    debug:
#      msg: "Secret password is {{ansible_ssh_pass}}"
  - name: upgrade dist!
    apt:
    upgrade: dist
  - name: try to install inotify-tools
    apt:
      name: inotify-tools
      state: latest
      update_cache: yes
      allow_unauthenticated: yes
#create variable from command
  - name: I am confused
    command: 'whoami'
    become: true
    become_method: sudo
    become_user: root
    register: myidentity
  - name: my secret identity
    debug:
      msg: '{{ myidentity.stdout }}'      
#        var: hostvars[ansible_ssh_pass]    

#  - name: Ensure that sshd is started
#    ansible.builtin.service:
#      name: sshd
#      state: started
      
#ansible-playbook --inventory-file "ansible_hosts.yml" play-install-terminal.yml


